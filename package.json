{
  "name": "test_electron_autoupdate",
  "version": "1.2.0",
  "description": "test for electron autoupdate",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "electron-builder build --win --publish never",
    "deploy": "electron-builder build --win --publish always"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "electron-updater": "^4.2.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/gandol2/test_electron_autoupdate.git"
  },
  "devDependencies": {
    "electron": "^7.2.4",
    "electron-builder": "^22.1.0"
  }
}
